<div class='glyph'> 
	<p id = '<%= glyph.id %>'>
		<%= image_tag "#{Rails.configuration.x.img_url}#{glyph.url}.png", title: glyph.meaning, width: '40', class: "glyph" %><br>
		<%= glyph.meaning %>
	</p>
</div> 